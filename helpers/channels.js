var ChannelList = {
    friends: {
        channel: "private-chat",
        listen: ".friends"
    },
    postReaction: {
        channel: "postReaction.",
        listen: ".reaction"
    }
}

export { ChannelList }