var ChannelList = {
    friends: {
        channel: "friendRequestSent.",
        listen: ".recieved"
    },
    postReaction: {
        channel: "postReaction.",
        listen: ".reaction"
    }
}

export { ChannelList }